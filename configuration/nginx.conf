server {
    listen 80;
    server_name api.meipu-ai.cn;

    root /var/data/audio;
    autoindex on;
    autoindex_exact_size off;
    autoindex_localtime on;
    charset utf-8;

    location /audios {
        try_files $uri $uri/ =404;
    }
}
